import React from "react";

const FullName = ({ name }) => {
    return (
        <div>
            <h1 style={{ fontSize: "50px", color: "white" }}>{name}</h1>
        </div>
    );
};

export default FullName;
